var bitcoin = require('bitcoinjs-lib');

module.exports = Colu;

function Colu(privateKey, cc, i) {
  
  
}

Colu.register(callback) {
  
}

Colu.createRegistrationMessage(username) {
  
}

Colu.createRegistrationQR(registrationMessage) {
  
}

Colu.registerUser(registrationMessage) {
  
}
